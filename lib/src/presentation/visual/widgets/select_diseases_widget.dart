import 'package:flutter/material.dart';

class SelectDiseases extends StatelessWidget {
  const SelectDiseases({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
